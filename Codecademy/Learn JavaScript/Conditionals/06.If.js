//first if statement
let sale = true;
//sale = false;
// if statements expect the conditional as true.
//when you uncomment line 3 it prevents if statement from running.
if (sale) {
  console.log('Time to buy!');
};
